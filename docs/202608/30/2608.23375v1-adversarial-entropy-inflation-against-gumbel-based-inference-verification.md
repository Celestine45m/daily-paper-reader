---
title: Adversarial Entropy Inflation Against Gumbel-Based Inference Verification
title_zh: 对抗性熵膨胀对基于Gumbel的推理验证的攻击
authors: Nikita Kezins
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23375v1"
tags: ["query:as"]
score: 7.0
evidence: 基于文本提示的对抗攻击，针对LLM推理验证机制
tldr: Gumbel验证通过只容忍符合诚实GPU不确定性的token选择来限制LLM权重外泄，但该防御假设被动攻击者。我们证明若攻击者控制提示分布，利用破坏语法和子词结构的提示可扩大可接受token集，显著增大隐蔽信道。在1B至32B六个模型上，最强攻击使每token泄露比特翻倍，将放缓因子降至60-118倍。静态阈值不足，应基于局部token熵动态校准抖动容忍阈值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.23375v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1418, \"height\": 1229, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.23375v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1438, \"height\": 268, \"label\": \"Table\"}]"
motivation: 现有Gumbel验证的阈值基于良性提示校准，未考虑主动控制提示分布的对抗者，导致防御边界被高估。
method: 通过设计字符级和脚本级破坏的提示，增大模型输出熵以扩大可接受token集，从而提升每token泄露比特数。
result: 在六个指令微调模型上，攻击使比特泄露约翻倍，放缓因子从200倍以上降至60-118倍，验证了防御显著退化。
conclusion: 静态良性流量校准的阈值不可靠，需根据局部token熵动态调整jitter容忍阈值以抵御主动攻击。
---

## 摘要
基于Gumbel的推理验证通过仅允许那些合理源于GPU诚实非确定性的令牌选择，来限制LLM权重窃取，并报告在良性提示流量下对隐写术攻击者产生超过200倍的减速。该界限假设攻击者是被动的；我们表明，当攻击者转而控制提示分布时，其防御效果急剧下降。由于验证器的可接受令牌集大小由模型自身的输出熵决定，因此旨在打破语法和子词结构（而非良性对话流量）的提示会扩大该集合，并开辟一个实质性更大的隐蔽信道。在六个指令调优模型（参数从10亿到320亿）和三个随机种子中，我们最强的攻击（字符级和脚本级破坏）相对于良性提示，每令牌泄露的比特数大约翻倍，将减速因子降至60倍至118倍。这些结果表明，静态的、基于良性流量校准的阈值不足以支撑这一防御，而抖动宽恕阈值应针对局部令牌熵进行动态校准。

## Abstract
Gumbel-based inference verification bounds LLM weight exfiltration by only forgiving token choices that plausibly arise from honest GPU nondeterminism, reporting a >200x slowdown for a steganographic adversary under benign prompt traffic. This bound assumes a passive attacker; we show it degrades sharply against an adversary who instead controls the prompt distribution. Because the verifier's admissible-token-set size is driven by the model's own output entropy, prompts engineered to break grammatical and sub-word structure -- rather than benign conversational traffic -- widen that set and open a materially larger covert channel. Across six instruction-tuned models spanning 1B to 32B parameters and three random seeds, our strongest attack (character- and script-level disruption) roughly doubles bits leaked per token relative to benign prompts, cutting the slowdown factor to 60x - 118x. These results indicate that static, benign-traffic-calibrated thresholds are insufficient for this defense, and that jitter-forgiveness thresholds should instead be calibrated dynamically against local token entropy.